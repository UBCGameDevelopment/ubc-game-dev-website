---
import EventTimelineReact from "./EventTimeline.tsx";

interface Props {
    index: number;
    isLast: boolean;
}

const { index, isLast } = Astro.props;
---

<EventTimelineReact
    index={index}
    isLast={isLast}
    client:load
/>
