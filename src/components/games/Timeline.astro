---
import TimelineReact from "./Timeline.tsx";
import { getGameYears } from "../../data/games";

const gameYears = getGameYears();
const years = ["All Years", ...gameYears];
const initialYear = "All Years";
---

<TimelineReact
    years={years}
    initialYear={initialYear}
    client:load
/>
